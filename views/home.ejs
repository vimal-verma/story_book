<!DOCTYPE html>
<html lang="en">
    <%- include('components/head.ejs'); -%>
<body>
    <div class="container">
    <%- include('components/header.ejs'); -%>
    <h1>My Story</h1>
    <% story.forEach(story => { %>
    <div class="card mb-3">
            <div class="card-body">
              <h5 class="card-title"><%= story.title %> </h5>
              <p class="card-text"><%= story.body %> </p>
              <p class="card-text"><small class="text-muted">At <%= story.createdAt %></small></p>
        </div>
      </div>
    <% }); %>
    </div>

    <%- include('components/footer.ejs'); -%>
</body>
</html>